<h2>New Post</h2>
<% if @post.errors.any? %>
	<% @post.errors.full_messages.each do |message| %>
		<h3><%= message %></h3>
	<% end %>
<% end%>
<%= form_with model: @post do |form| %>
	<%= form.text_area :body, size: "60x10" %>
	<%= form.submit %>
<% end %>
